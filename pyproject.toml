[project]
name = "functional-tests"
version = "0.1.0"
description = "Standalone test scripts for validating Claude API features on Amazon Bedrock and the Anthropic 1st-party API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.74.1",
    "boto3>=1.41.1",
    "claude-code-sdk>=0.0.25",
    "pillow>=12.1.1",
    "pyyaml>=6.0.3",
]
